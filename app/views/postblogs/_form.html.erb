<%= form_with(model: postblog, local: true) do |form| %>
  <% if postblog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(postblog.errors.count, "error") %> prohibited this postblog from being saved:</h2>

      <ul>
        <% postblog.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :text %>
    <%= form.text_field :text %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
